# dependencies / build
node_modules/
dist/
*.tsbuildinfo

# local runtime state (never commit machine-specific captures)
.headroom/
*.local.json

# spike captures
**/raw-sample.json

# logs / env / credentials (tokens live in ~/.npmrc and repo secrets, never here)
*.log
.env
.env.*
.npmrc

# editor / os
.DS_Store
.idea/
.vscode/
eval/v1/cells/
eval/v2-continuity/cells/

# launch drafts (not for the public repo)
launch/

# eval cell outputs are runtime artifacts — regenerate via setup-cell scripts
eval/v2-continuity/cells-large/

# orchestrator working state — deliberately untracked (its own header says do-not-commit)
ORCHESTRATION.md
